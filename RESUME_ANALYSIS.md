# 简历项目分析报告

## 项目名称
**Comment** - 基于 Redis + Kafka 的高并发电商秒杀系统

## 技术栈关键词
`Spring Boot 2.7` `Redis` `Redisson` `Kafka` `MySQL` `MyBatis-Plus` `Lua Script` `分布式锁` `异步解耦` `缓存穿透/击穿解决方案` `全局唯一ID生成器` `GEO地理位置` `BitMap签到` `Feed流推送`

---

## 简历核心要点（STAR 法则）

### 📌 要点一：基于 Redis + Lua + Kafka 的高并发秒杀系统

**Situation（情境）：**
在电商秒杀场景中，面临瞬时高并发请求（如万级QPS），传统同步下单方式会导致数据库压力过大、超卖、一人多单等问题。

**Task（任务）：**
设计一个高性能、高可用的秒杀系统，解决超卖、一人一单、库存一致性等核心问题，同时保证系统吞吐量。

**Action（行动）：**
- **使用 Lua 脚本保证原子性**：将库存校验、一人一单校验、扣减库存三个操作封装在 `seckill.lua` 脚本中，通过 Redis 的单线程特性保证原子性执行，避免超卖和重复下单问题
- **采用 Kafka 消息队列异步解耦**：秒杀下单请求在 Redis 预扣库存成功后，将订单信息发送到 Kafka 队列，由消费者异步写入数据库，实现读写分离和流量削峰
- **使用 Redisson 分布式锁保证幂等性**：消费者处理订单时，通过 `RLock` 基于用户ID加锁，防止消息重复消费导致的重复下单

**Result（结果）：**
- 通过 Redis 内存操作 + Lua 原子校验，将秒杀请求响应时间从数据库同步操作级别理论上降低到毫秒级（预估提升 5-10 倍，具体取决于数据库和网络环境）
- 数据库写压力通过异步削峰显著降低，避免瞬时高并发打垮数据库
- 实现了库存零超卖、一人一单的强一致性保证

**代码位置：** `VoucherOrderServiceImpl.java`, `seckill.lua`

---

### 📌 要点二：多级缓存策略解决缓存穿透与击穿问题

**Situation（情境）：**
在高并发商铺查询场景下，缓存失效瞬间大量请求同时打到数据库（缓存击穿），或恶意请求查询不存在的数据（缓存穿透），可能导致数据库崩溃。

**Task（任务）：**
设计一套完整的缓存解决方案，防止缓存穿透和击穿，保证系统高可用。

**Action（行动）：**
- **缓存穿透解决方案**：对数据库查询结果为空的情况，缓存空值（`""`）并设置较短 TTL，避免恶意请求穿透到数据库
- **缓存击穿解决方案（互斥锁）**：采用 SETNX 实现分布式互斥锁，只允许一个线程查询数据库并重建缓存，其他线程休眠重试，并采用 **Double Check（双重校验）** 避免重复查库
- **缓存击穿解决方案（逻辑过期）**：对热点数据采用逻辑过期策略，数据永不过期，但记录逻辑过期时间。过期后通过异步线程池重建缓存，保证请求不阻塞
- **封装通用工具类**：将上述策略封装为 `CacheClient` 工具类，支持泛型和函数式编程，提供 `queryWithPassThrough`、`queryWithMutex`、`queryWithLogicalExpire` 等通用方法

**Result（结果）：**
- 有效防止缓存穿透攻击，减少无效数据库查询
- 缓存击穿场景下，数据库查询次数从 N 降低到 1（N 为并发请求数）
- 通过策略模式和泛型封装，实现代码复用，降低开发成本

**代码位置：** `CacheClient.java`, `ShopServiceImpl.java`

---

### 📌 要点三：分布式锁实现与 Lua 脚本保证释放原子性

**Situation（情境）：**
在分布式环境下，传统的 synchronized 或 ReentrantLock 无法跨 JVM 实现互斥，需要分布式锁来保证临界区操作的原子性。

**Task（任务）：**
实现一个可靠的分布式锁机制，解决锁误删问题，并对比自研锁与 Redisson 的差异。

**Action（行动）：**
- **自研 SimpleRedisLock**：
  - 基于 Redis 的 SETNX 命令实现获取锁
  - 使用 `UUID + 线程ID` 作为锁标识，解决集群环境下的锁误删问题
  - **通过 Lua 脚本** (`unlock.lua`) 实现「比较锁标识」和「释放锁」的原子操作，防止在判断后、删除前锁被其他线程获取的竞态条件
- **引入 Redisson 分布式锁**：
  - 使用 Redisson 的 `RLock` 替代自研锁，获得 Watchdog 自动续期、可重入锁等高级特性
  - 在 Kafka 消费者中使用 Redisson 锁保证消息消费的幂等性

**Result（结果）：**
- Lua 脚本保证释放锁的原子性，彻底解决锁误删导致的并发安全问题
- Redisson 的 Watchdog 机制避免业务执行时间过长导致锁提前释放
- 代码演进路径清晰，展示了从自研到成熟框架的技术选型过程

**代码位置：** `SimpleRedisLock.java`, `unlock.lua`, `VoucherOrderServiceImpl.java`

---

### 📌 要点四：基于 Redis 的全局唯一 ID 生成器（雪花算法变体）

**Situation（情境）：**
分布式系统中，数据库自增 ID 存在单点瓶颈，且易暴露业务量信息，需要高性能、全局唯一、趋势递增的 ID 生成方案。

**Task（任务）：**
设计一个基于 Redis 的全局唯一 ID 生成器，满足高并发场景下的订单号生成需求。

**Action（行动）：**
- **ID 结构设计**：采用 64 位 Long 型，前 32 位存储时间戳（秒级），后 32 位存储 Redis 自增序列号
- **Redis 自增键设计**：按 `icr:{业务前缀}:{日期}` 格式设计键名，既支持按日统计订单量，又避免单个键值过大
- **位运算拼接**：通过左移 32 位 + 或运算，将时间戳和序列号高效拼接

**Result（结果）：**
- 单日单业务前缀理论支持 2^32（约 42 亿）个唯一 ID，满足绝大多数业务场景
- ID 趋势递增，利于数据库索引优化
- 通过日期分片，便于按日统计业务数据，支持数据分析需求

**代码位置：** `RedisIdWorker.java`

---

## 技术亮点补充

| 功能模块 | 技术实现 | 解决的问题 |
|---------|---------|-----------|
| 用户签到 | Redis BitMap | 空间复杂度降低到 1/8，单用户一年仅需 46 字节 |
| 连续签到统计 | 位运算 | 高效计算连续签到天数，时间复杂度 O(n) |
| 关注/共同关注 | Redis Set 交集 | SINTER 命令实现 O(n) 共同关注查询 |
| 点赞排行榜 | Redis ZSet | SCORE 记录点赞时间，支持按时间排序 |
| Feed 流推送 | Redis ZSet + 滚动分页 | 实现基于时间线的关注动态推送 |
| 附近商铺 | Redis GEO | GEOSEARCH 实现按距离排序的地理位置查询 |
| Token 刷新 | 拦截器链 | 双层拦截器实现无感 Token 续期 |

---

## 设计模式总结

1. **策略模式**：`CacheClient` 提供多种缓存策略（穿透、击穿-互斥锁、击穿-逻辑过期）
2. **模板方法模式**：`CacheClient` 使用函数式接口 `Function<ID, R>` 实现数据库查询逻辑的回调
3. **责任链模式**：`RefreshTokenInterceptor` 和 `LoginInterceptor` 组成拦截器链
4. **单例模式**：Lua 脚本通过静态代码块加载，全局共享
5. **生产者-消费者模式**：Kafka 实现秒杀订单的异步处理

---

## 面试加分项

1. **锁优化演进**：从方法级 synchronized → synchronized + String.intern()（缩小锁粒度到用户级别）→ 自研 Redis 分布式锁 → Redisson，展示技术深度
2. **超卖问题方案对比**：乐观锁（版本号/CAS）vs 条件更新 vs Lua 脚本原子操作
3. **异步方案对比**：JVM 阻塞队列 → Redis Stream → Kafka，展示消息队列选型思路
4. **缓存一致性**：采用 Cache Aside 模式（先更新 DB，再删除缓存）

---

*本文档基于代码仓库自动分析生成，用于简历撰写和面试准备参考*
